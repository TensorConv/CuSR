# compiled binaries
batch_lm
batch_lm_ad
batch_lm_ad_*
batch_lm_noguard
batch_lm_devjac
batch_lm_fusedfd
inspect

# in-process CO drop-in shared libs (P1) — build artifacts (make libcusr_co_*.so)
libcusr_co_fd.so
libcusr_co_ad.so
libcusr_co_fd_prof.so
libcusr_co_ad_prof.so

# test run outputs
tests/_out/
tests/_out_op/
tests/_out_stress/
tests/_out_scale/

# Python
__pycache__/
*.pyc

# Internal-only notes (detailed engineering README, host↔GPU dialog,
# roadmap, etc.). Kept locally for our own reference, not shipped publicly.
INTERNAL.md

# Local learning walkthrough — incremental .cu ladder for understanding
# batch_lm.cu step by step. Personal study material, not shipped publicly.
/walkthrough/
batch_lm_fusedfd_*

# clangd (clangd-lsp plugin) tooling — machine-specific, not shipped.
# compile_commands.json holds absolute paths (/home/.../cuda, host-math shim);
# .cache/clangd is the background index. Regenerate per-machine.
compile_commands.json
.cache/
